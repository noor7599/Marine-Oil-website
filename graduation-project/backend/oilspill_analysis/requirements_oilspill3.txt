# Main Pipeline Environment (oilspill3) - Python 3.9
# Install with: conda activate oilspill3 && pip install -r requirements_oilspill3.txt

# Core data processing (numpy pinned to avoid opencv-python conflicts)
numpy==1.26.4
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Geospatial and remote sensing
rasterio>=1.2.0
Pillow>=8.0.0

# Ocean simulation (pinned for Python 3.9 compatibility)
opendrift==1.11.0
xarray>=0.18.0
cmocean>=2.0,<3.0

# Copernicus data download
cdsapi==0.7.7
copernicusmarine>=2.0.0

# Machine learning utilities
joblib>=1.0.0

# Visualization (pinned for matplotlib >= 3.5 compatibility with cmocean 2.0+)
matplotlib==3.5.3

# Utilities
python-dateutil>=2.8.0

# NLP / incident validation
geopy>=2.4.0
reportlab>=3.6.0
fpdf2>=2.7.0

# Debugging  
tqdm>=4.60.0
pyyaml>=5.3.0
